[project]
name = "fullstack-bridge"
version = "0.1.0"
description = "前后端连通层：把单端模板与组合契约整合为前后端一体大目录（全 copier 工具链）"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

# 非发布型工具项目：uv sync 只装依赖，不构建/安装本项目
[tool.uv]
package = false
